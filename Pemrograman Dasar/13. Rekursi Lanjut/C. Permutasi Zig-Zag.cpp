#include <cstdio>
#include <iostream>
using namespace std;

#define ll long long

// Unfinished.

ll N;

int main() {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    
    cin >> N;
    vector<bool> done(N);
    for(bool &x : b) x = false;

    
    return 0;
}
void write(int depth) {
    if (depth >= N) {
        bool z = true;
        for (int i=2; i<N; ++i) {
            bool
        }
    }
}